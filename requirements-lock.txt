# ==============================================================================
# 1. EXACT HISTORICAL PRODUCTION RUNTIME PINS
# Proven by accepted final P70 runner (p70_public1000_dual_t4_runner_r3.py:L110)
# ==============================================================================
transformers==5.17.0
peft==0.20.0
accelerate==1.15.0
bitsandbytes==0.50.2
# Note: torchao was explicitly uninstalled/removed in the accepted production runtime.

# ==============================================================================
# 2. REPRODUCTION-ENVIRONMENT CONVENIENCE PINS
# Required for a functional, installable public environment. These packages were
# installed unpinned (e.g. `pip install safetensors`) or preinstalled by the host
# (Kaggle Python 3.10+ image with PyTorch 2.x; Modal H100 with torch 2.14.0).
# The exact versions below are reproducibility choices, NOT historical exact pins.
# ==============================================================================
torch==2.6.0
safetensors==0.7.0
sentence-transformers==3.4.1
scikit-learn==1.6.1
numpy==2.2.3
pyyaml==6.0.2
nltk==3.9.1
pytest==8.3.4
